"""
Legacy modules — frozen, not for new code.

These modules are preserved for backward compatibility. New code must not
add dependencies on legacy modules. Each commit should reduce legacy line count.
"""
